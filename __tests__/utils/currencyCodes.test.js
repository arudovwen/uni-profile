import Currency from '~/utils/countrycodes';

describe('Constants', () => {
    test('Currency should be defined and have correct structure', () => {
      expect(Currency).toBeDefined();
     
    });
})// Adjust the import path based on your actual file structure
  